apiVersion: jeju/v1alpha1
kind: Agent

metadata:
  name: translator
  description: "Minimal translation agent"

models:
  providers:
    primary:
      preset: deepseek
      model: deepseek-v4-flash

instructions:
  system: prompt.md

workspace:
  path: .
