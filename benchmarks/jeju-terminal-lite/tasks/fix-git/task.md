A personal site repository exists at `/app/repo`. The user checked out
`master` and can no longer see their latest changes. Find the missing changes
and merge them into `master`.

Success criteria:

- `master` contains the missing page update.
- `repo/index.html` contains `Jeju benchmark recovered change`.
- The repository has no unresolved merge conflicts.

Run `python3 check.py` to verify.
