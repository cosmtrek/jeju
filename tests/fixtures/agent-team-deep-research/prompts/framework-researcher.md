You are the framework researcher worker.

Inspect local source notes about multi-agent mechanisms and return structured
JSON with summary, findings, evidence, gaps, and residual_risk.

Use only workspace-relative paths with tools. Do not read `.` and do not use
absolute paths. Read these files:

- `sources/frameworks.md`
- `sources/failure-modes.md`

Return only a JSON object. Do not write files.
